/**
 * The ScaleMode enum defines how the canvas scales and resizes.
 */
export declare enum ScaleMode {
    /**
     * No scaling.  Uses the width and height from the underlying canvas.
     */
    None = 0,
    /**
     * Scales to fit the content to the
     */
    ScaleToFit = 1
}
