{
  "name": "root",
  "version": "2.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "npm run dev --workspace=wglt-docs",
    "fix": "biome check --write --unsafe .",
    "lint": "biome check .",
    "preview": "npm run preview --workspace=wlgt-docs",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "22.12.0",
    "turbo": "2.3.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.9.0"
}
